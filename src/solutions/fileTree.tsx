import React from 'react';

const FileTree: React.FC<Props> = ({ ...props }) => {

    return (
        <React.Fragment>
            File Tree
        </React.Fragment>
    );
}

export default FileTree;

interface Props {
    
}