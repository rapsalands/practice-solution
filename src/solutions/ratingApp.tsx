import React from 'react';

const RatingApp: React.FC<Props> = ({ ...props }) => {

    return (
        <React.Fragment>
            Rating App
        </React.Fragment>
    );
}

export default RatingApp;

interface Props {
    
}