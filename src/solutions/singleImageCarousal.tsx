import React from 'react';

const SingleImageCarousal: React.FC<Props> = ({ ...props }) => {

    return (
        <React.Fragment>
            Single Image Carousal
        </React.Fragment>
    );
}

export default SingleImageCarousal;

interface Props {
    
}